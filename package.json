{
  "private": true,
  "scripts": {
    "commit": "git-cz",
    "clean": "lerna run clean",
    "format": "prettier --write '*.{js,yml,yaml,json}' 'packages/**/*.{js,ts,tsx,yml,yaml,json}'",
    "lint": "tslint --project tsconfig.json -c tslint.json -t codeFrame packages/**/*.ts{,x}",
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --watch --noEmit",
    "test": "jest",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "7.5.0",
    "@commitlint/config-lerna-scopes": "7.5.0",
    "@monstrs/github-checks-cli": "1.1.3",
    "@monstrs/github-checks-formatter-jest": "1.1.3",
    "@monstrs/github-checks-formatter-tsc": "1.1.3",
    "@monstrs/github-checks-formatter-tslint": "1.1.3",
    "@types/jest": "24.0.9",
    "codecov": "3.2.0",
    "commitizen": "3.0.7",
    "cz-conventional-changelog": "2.1.0",
    "cz-lerna-changelog": "2.0.2",
    "husky": "1.3.1",
    "jest": "24.1.0",
    "lerna": "3.13.1",
    "lint-staged": "8.1.4",
    "prettier": "1.16.4",
    "ts-jest": "24.0.0",
    "tslint": "5.13.1",
    "tslint-config-prettier": "1.18.0",
    "typescript": "3.3.3333"
  },
  "workspaces": [
    "packages/*"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "yarn lint && yarn test"
    }
  },
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "yarn format",
      "git add"
    ]
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-lerna-changelog"
    }
  }
}
